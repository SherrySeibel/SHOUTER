<% if target.errors.any? %>
  <%= pluralize(target.errors.count, "error") %> prevented the form from submitting:
  <ul>
    <% target.errors.full_messages.each do |error_message| %>
      <li><%= error_message %></li>
    <% end %>
  </ul>
<% end %>
